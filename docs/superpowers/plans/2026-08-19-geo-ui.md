# Geo UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** LAN-веб UI на роутере для CRUD групп в `/opt/root/geo/geo.d/` с немедленным `geo-update.sh -n` и живым логом в консоли UI.

**Architecture:** Python Flask слушает IPv4 `br0:8787`, отдаёт Vue `dist/` и JSON/SSE API. Группа = один `.conf`. После мутации — flock + `geo-update.sh -n`, stdout/stderr построчно в SSE. Ручные IPv4/CIDR пишет `geo-update.sh` в `*-ip`.

**Tech Stack:** Python 3.9+ / Flask 2.x, Vue 3 + Vite (сборка на ПК), POSIX sh (`geo-update.sh`), Entware init.d.

## Global Constraints

- Bind только LAN IPv4 (`br0`), никогда `0.0.0.0`, никогда WAN/`nwg0`.
- Порт `8787`; другой порт — только правка init.d.
- Сеты только `blocked-sites` и `only-ru`. IPv6 нет.
- Префикса `ip:` нет: IPv4/CIDR — голая строка в секции.
- Title = первая `#` до секции; иначе имя файла без `.conf`. Остальные `#` заголовка = description (может быть пустым).
- Мутации после записи файла отвечают `text/event-stream`; GET и 400/404 — `application/json`.
- Коллизии подсвечивать, не блокировать сохранение.
- Git на роутере не трогать. Коммиты в репо — только если пользователь явно попросил (шаги Commit пропускать до просьбы).
- Visual design UI прорабатывается в Task 8 до вёрстки Vue.
- Диагностика `geo-why` — не в этой версии.
- Тесты backend: `python -m pytest` из `scripts/geo-ui` (Windows PC). `geo-update.sh` smoke — на роутере в Task 10.

## File map

| Path | Responsibility |
|---|---|
| `scripts/geo/geo-update.sh` | Парсер CIDR + заливка `*-ip` без обязательного `geoip:` |
| `scripts/geo-ui/backend/confio.py` | parse/write `geo.d/*.conf`, classify, slug |
| `scripts/geo-ui/backend/collisions.py` | Точные совпадения value по группам/сетам |
| `scripts/geo-ui/backend/apply.py` | flock + subprocess `geo-update.sh -n`, построчный stdout+stderr |
| `scripts/geo-ui/backend/bindaddr.py` | IPv4 LAN для Flask.bind |
| `scripts/geo-ui/backend/tags.py` | Список тегов geoview |
| `scripts/geo-ui/backend/app.py` | Flask: static dist + API |
| `scripts/geo-ui/backend/tests/` | pytest |
| `scripts/geo-ui/frontend/` | Vue 3 + Vite |
| `scripts/geo-ui/frontend/UI.md` | Согласованный визуальный дизайн |
| `scripts/geo-ui/init.d/S80geo-ui` | Entware start/stop |
| `scripts/geo-ui/requirements.txt` | flask |

---

### Task 1: confio — classify, parse, write

**Files:**
- Create: `scripts/geo-ui/backend/confio.py`
- Create: `scripts/geo-ui/backend/tests/test_confio.py`
- Create: `scripts/geo-ui/requirements.txt`
- Create: `scripts/geo-ui/pytest.ini`

**Interfaces:**
- Consumes: формат файлов из spec
- Produces:
  - `SETS = ("blocked-sites", "only-ru")`
  - `Entry(set_name: str, value: str, kind: str)` где `kind` ∈ `domain|geosite|geoip|cidr`
  - `Group(slug: str, path: Path, title: str, description: str, title_from_file: bool, entries: list[Entry])`
  - `classify_line(line: str) -> str | None` — `geosite|geoip|cidr|domain` или `None` если мусор
  - `parse_file(path: Path) -> Group`
  - `write_file(group: Group) -> None`
  - `list_groups(dir: Path) -> list[Group]`
  - `slugify(title: str) -> str`
  - `unique_slug(dir: Path, title: str) -> str`

- [ ] **Step 1: Write the failing tests**

```python
# scripts/geo-ui/backend/tests/test_confio.py
from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from confio import classify_line, parse_file, write_file, slugify, unique_slug, Group, Entry

def test_classify():
    assert classify_line("geosite:youtube") == "geosite"
    assert classify_line("geoip:telegram") == "geoip"
    assert classify_line("1.2.3.4") == "cidr"
    assert classify_line("10.0.0.0/8") == "cidr"
    assert classify_line("example.com") == "domain"
    assert classify_line("playstation") == "domain"
    assert classify_line("999.1.1.1") is None
    assert classify_line("not a domain!") is None

def test_parse_title_description_and_fallback(tmp_path: Path):
    p = tmp_path / "sony.conf"
    p.write_text(
        "# PlayStation / Sony\n# Домены PSN\n\n[blocked-sites]\nplaystation.com\n",
        encoding="utf-8",
    )
    g = parse_file(p)
    assert g.slug == "sony"
    assert g.title == "PlayStation / Sony"
    assert g.description == "Домены PSN"
    assert g.title_from_file is True
    assert g.entries == [Entry("blocked-sites", "playstation.com", "domain")]

    q = tmp_path / "custom.conf"
    q.write_text("[only-ru]\nmyip.ru\n", encoding="utf-8")
    g2 = parse_file(q)
    assert g2.title == "custom"
    assert g2.description == ""
    assert g2.title_from_file is False

def test_write_roundtrip_mixed(tmp_path: Path):
    p = tmp_path / "mix.conf"
    g = Group(
        slug="mix",
        path=p,
        title="Mix",
        description="desc line",
        title_from_file=True,
        entries=[
            Entry("blocked-sites", "a.com", "domain"),
            Entry("blocked-sites", "geosite:qt", "geosite"),
            Entry("blocked-sites", "1.2.3.4/24", "cidr"),
            Entry("only-ru", "myip.ru", "domain"),
        ],
    )
    write_file(g)
    g2 = parse_file(p)
    assert g2.title == "Mix"
    assert g2.description == "desc line"
    assert [(e.set_name, e.value, e.kind) for e in g2.entries] == [
        ("blocked-sites", "a.com", "domain"),
        ("blocked-sites", "geosite:qt", "geosite"),
        ("blocked-sites", "1.2.3.4/24", "cidr"),
        ("only-ru", "myip.ru", "domain"),
    ]

def test_write_without_header_stays_headerless_until_titled(tmp_path: Path):
    p = tmp_path / "x.conf"
    p.write_text("[blocked-sites]\na.com\n", encoding="utf-8")
    g = parse_file(p)
    g.entries.append(Entry("blocked-sites", "b.com", "domain"))
    write_file(g)
    text = p.read_text(encoding="utf-8")
    assert not text.startswith("#")
    assert "b.com" in text

def test_slugify_and_unique(tmp_path: Path):
    assert slugify("PlayStation / Sony") == "playstation-sony"
    (tmp_path / "playstation-sony.conf").write_text("[blocked-sites]\na.com\n", encoding="utf-8")
    assert unique_slug(tmp_path, "PlayStation / Sony") == "playstation-sony-2"
```

`scripts/geo-ui/pytest.ini`:

```ini
[pytest]
testpaths = backend/tests
pythonpath = backend
```

`scripts/geo-ui/requirements.txt`:

```
flask>=2.3,<4
pytest>=7
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd scripts/geo-ui && python -m pytest backend/tests/test_confio.py -v`

Expected: FAIL (`ModuleNotFoundError: confio` или import error).

- [ ] **Step 3: Write minimal implementation**

`scripts/geo-ui/backend/confio.py` — полный модуль:

```python
from __future__ import annotations

import re
from dataclasses import dataclass, field
from pathlib import Path

SETS = ("blocked-sites", "only-ru")
SET_RE = re.compile(r"^[A-Za-z0-9][A-Za-z0-9_-]{0,30}$")
DOMAIN_RE = re.compile(
    r"^([A-Za-z0-9]([A-Za-z0-9-]*[A-Za-z0-9])?\.)+[A-Za-z]{2,}$"
)
TLD_RE = re.compile(r"^[A-Za-z]{2,}$")
CIDR_RE = re.compile(
    r"^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}"
    r"(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)"
    r"(/(3[0-2]|[12]?[0-9]))?$"
)
SLUG_RE = re.compile(r"[^a-z0-9]+")


@dataclass
class Entry:
    set_name: str
    value: str
    kind: str


@dataclass
class Group:
    slug: str
    path: Path
    title: str
    description: str
    title_from_file: bool
    entries: list[Entry] = field(default_factory=list)


def classify_line(line: str) -> str | None:
    s = line.strip()
    if s.startswith("geosite:") and s[8:].strip():
        return "geosite"
    if s.startswith("geoip:") and s[6:].strip():
        return "geoip"
    if CIDR_RE.match(s):
        return "cidr"
    if DOMAIN_RE.match(s) or TLD_RE.match(s):
        return "domain"
    return None


def parse_file(path: Path) -> Group:
    slug = path.stem
    title = slug
    title_from_file = False
    desc_parts: list[str] = []
    entries: list[Entry] = []
    cur_set = ""
    in_header = True
    for raw in path.read_text(encoding="utf-8").splitlines():
        stripped = raw.strip().replace("\r", "")
        if in_header:
            if not stripped:
                continue
            if stripped.startswith("#"):
                c = stripped[1:].strip()
                if not title_from_file:
                    title = c or slug
                    title_from_file = True
                elif c:
                    desc_parts.append(c)
                continue
            in_header = False
        if not stripped or stripped.startswith("#"):
            continue
        line = stripped.split("#", 1)[0].strip()
        if not line:
            continue
        if line.startswith("[") and line.endswith("]"):
            s = line[1:-1].replace(" ", "")
            cur_set = s if SET_RE.match(s) else ""
            continue
        if line.startswith("include"):
            continue
        kind = classify_line(line)
        if not kind or not cur_set:
            continue
        entries.append(Entry(cur_set, line, kind))
    return Group(slug, path, title, "\n".join(desc_parts), title_from_file, entries)


def write_file(group: Group) -> None:
    lines: list[str] = []
    if group.title_from_file:
        lines.append("# " + group.title)
        if group.description:
            for d in group.description.split("\n"):
                lines.append("# " + d)
        lines.append("")
    by_set: dict[str, list[str]] = {s: [] for s in SETS}
    extra: dict[str, list[str]] = {}
    for e in group.entries:
        val = e.value
        (by_set if e.set_name in by_set else extra.setdefault(e.set_name, [])).append(val)
    for s in SETS:
        if not by_set[s]:
            continue
        lines.append("[" + s + "]")
        lines.extend(by_set[s])
        lines.append("")
    for s, vals in extra.items():
        lines.append("[" + s + "]")
        lines.extend(vals)
        lines.append("")
    group.path.write_text("\n".join(lines).rstrip() + "\n", encoding="utf-8")


def list_groups(dir_path: Path) -> list[Group]:
    return [parse_file(p) for p in sorted(dir_path.glob("*.conf"))]


def slugify(title: str) -> str:
    s = SLUG_RE.sub("-", title.lower()).strip("-")
    return s or "group"


def unique_slug(dir_path: Path, title: str) -> str:
    base = slugify(title)
    slug = base
    n = 2
    while (dir_path / (slug + ".conf")).exists():
        slug = "%s-%d" % (base, n)
        n += 1
    return slug
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd scripts/geo-ui && python -m pip install -r requirements.txt && python -m pytest backend/tests/test_confio.py -v`

Expected: PASS (все 5 тестов).

- [ ] **Step 5: Commit** — пропустить, пока пользователь не попросил.

---

### Task 2: collisions

**Files:**
- Create: `scripts/geo-ui/backend/collisions.py`
- Create: `scripts/geo-ui/backend/tests/test_collisions.py`

**Interfaces:**
- Consumes: `Group`, `Entry` из `confio`
- Produces:
  - `Collision(value: str, hits: list[tuple[str, str]])` — hits = `(slug, set_name)`
  - `norm_value(entry: Entry) -> str`
  - `find_collisions(groups: list[Group]) -> list[Collision]` — только value, у которых `len(hits) > 1` или одно value в двух сетах

Одно вхождение не коллизия. Два файла с `example.com` — коллизия. `example.com` в `blocked-sites` и `only-ru` (даже в одной группе) — коллизия (only-ru победит).

- [ ] **Step 1: Write the failing tests**

```python
from pathlib import Path
from confio import Group, Entry
from collisions import find_collisions, norm_value

def _g(slug, entries):
    return Group(slug, Path(slug + ".conf"), slug, "", False, entries)

def test_norm_lower_domain_and_tags():
    assert norm_value(Entry("blocked-sites", "Example.COM", "domain")) == "example.com"
    assert norm_value(Entry("blocked-sites", "geosite:YouTube", "geosite")) == "geosite:youtube"
    assert norm_value(Entry("blocked-sites", "1.2.3.4/24", "cidr")) == "1.2.3.4/24"

def test_duplicate_across_groups():
    cs = find_collisions([
        _g("a", [Entry("blocked-sites", "a.com", "domain")]),
        _g("b", [Entry("blocked-sites", "A.com", "domain")]),
    ])
    assert len(cs) == 1
    assert cs[0].value == "a.com"
    assert {h[0] for h in cs[0].hits} == {"a", "b"}

def test_both_sets_is_collision():
    cs = find_collisions([
        _g("a", [
            Entry("blocked-sites", "a.com", "domain"),
            Entry("only-ru", "a.com", "domain"),
        ]),
    ])
    assert len(cs) == 1
    assert {h[1] for h in cs[0].hits} == {"blocked-sites", "only-ru"}

def test_unique_no_collision():
    assert find_collisions([
        _g("a", [Entry("blocked-sites", "a.com", "domain")]),
        _g("b", [Entry("blocked-sites", "b.com", "domain")]),
    ]) == []
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd scripts/geo-ui && python -m pytest backend/tests/test_collisions.py -v`

Expected: FAIL import `collisions`.

- [ ] **Step 3: Write minimal implementation**

```python
from __future__ import annotations

from dataclasses import dataclass
from collections import defaultdict
from confio import Entry, Group


@dataclass
class Collision:
    value: str
    hits: list[tuple[str, str]]


def norm_value(entry: Entry) -> str:
    v = entry.value.strip()
    if entry.kind in ("domain", "geosite", "geoip"):
        return v.lower()
    return v


def find_collisions(groups: list[Group]) -> list[Collision]:
    idx: dict[str, list[tuple[str, str]]] = defaultdict(list)
    for g in groups:
        for e in g.entries:
            idx[norm_value(e)].append((g.slug, e.set_name))
    out = []
    for value, hits in sorted(idx.items()):
        sets = {h[1] for h in hits}
        if len(hits) > 1 or len(sets) > 1:
            out.append(Collision(value, hits))
    return out
```

Замечание: `len(sets) > 1` при одном hit невозможно; условие `len(hits) > 1` достаточно (два сета = два hit). Оставить `len(hits) > 1`.

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd scripts/geo-ui && python -m pytest backend/tests/test_collisions.py -v`

Expected: PASS.

- [ ] **Step 5: Commit** — пропустить до просьбы.

---

### Task 3: geo-update.sh — ручные CIDR

**Files:**
- Modify: `scripts/geo/geo-update.sh`
- Test: на роутере в Task 10; regex должен совпасть с `CIDR_RE` в `confio.py`

**Interfaces:**
- Consumes: строки IPv4/CIDR в секциях
- Produces: CIDR в `$TMP/$set.cidrs` и swap `*-ip` даже без `geoip:` тегов

Текущий блок `*)` (домен) и блок `cf=... [ -s "$cf" ] || continue` — заменить.

- [ ] **Step 1: Добавить CIDR_RE рядом с DOMAIN_RE** (после строки `TLD_RE=...`):

```sh
CIDR_RE='^((25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.){3}(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)(/(3[0-2]|[12]?[0-9]))?$'
```

- [ ] **Step 2: При открытии секции создавать пустой `.cidrs`**

В ветке `\[*\])` сразу после `: >> "$TMP/$s.domains"` добавить:

```sh
                    : >> "$TMP/$s.cidrs"
                    : >> "$TMP/$s.geoip"
```

- [ ] **Step 3: В `*)` классифицировать CIDR раньше домена**

Заменить ветку `*)` на:

```sh
            *)
                if [ -z "$cur_set" ]; then warn "$f:$n: вне секции: $line"; continue; fi
                if printf '%s' "$line" | grep -qE "$CIDR_RE"; then
                    printf '%s\n' "$line" >> "$TMP/$cur_set.cidrs"
                elif printf '%s' "$line" | grep -qE "$DOMAIN_RE" || printf '%s' "$line" | grep -qE "$TLD_RE"; then
                    printf '%s\n' "$line" >> "$TMP/$cur_set.domains"
                else
                    warn "$f:$n: невалидная строка: $line"
                fi
                ;;
```

- [ ] **Step 4: Не `continue`, если нет geoip-тегов**

Заменить фрагмент от `cf="$TMP/$set_name.geoip"` до `[ -s "$cidrs" ] || continue` на:

```sh
    cidrs=$TMP/$set_name.cidrs
    cf="$TMP/$set_name.geoip"

    if [ -s "$cf" ]; then
        for tag in $(sort -u "$cf"); do
            log "$set_name: конвертирую geoip:$tag ..."
            if ! geoview -type geoip -action convert -input "$GEOIP_DAT" -list "$tag" \
                    -format ruleset -lowmem > "$TMP/gv.json" 2>"$TMP/geoview.err"; then
                warn "$set_name: geoip:$tag — ошибка geoview: $(head -c 200 "$TMP/geoview.err")"
                continue
            fi
            jq -r '.rules[].ip_cidr[]? | select(contains(":") | not)' \
                "$TMP/gv.json" 2>/dev/null | sort -u > "$CACHE/$set_name.geoip.$tag.cidr"
            cnt=$(wc -l < "$CACHE/$set_name.geoip.$tag.cidr")
            if [ "${cnt:-0}" -eq 0 ]; then
                warn "$set_name: geoip:$tag — пустой результат (тег существует?)"
            else
                cat "$CACHE/$set_name.geoip.$tag.cidr" >> "$cidrs"
                log "$set_name: geoip:$tag → $cnt CIDR (IPv4)"
            fi
        done
    fi

    [ -s "$cidrs" ] || continue
```

Не делать `: > "$cidrs"` — ручные CIDR уже записаны парсером. Остальной swap `ipset restore` не менять.

- [ ] **Step 5: Commit** — пропустить до просьбы.

---

### Task 4: bindaddr

**Files:**
- Create: `scripts/geo-ui/backend/bindaddr.py`
- Create: `scripts/geo-ui/backend/tests/test_bindaddr.py`

**Interfaces:**
- Consumes: вывод `ip -4 -o addr show` (строки `2: br0    inet 192.168.117.1/24 ...`)
- Produces: `lan_ipv4(addr_text: str, route_text: str = "") -> str` — поднимает `RuntimeError`, не возвращает `0.0.0.0`

Правило: если есть `br0` с inet — его IPv4. Иначе первая private (`10/8`, `172.16/12`, `192.168/16`) не `lo`, не `nwg0`, не iface из `ip route show default` (WAN).

- [ ] **Step 1: Write the failing tests**

```python
import pytest
from bindaddr import lan_ipv4

BR0 = "2: br0    inet 192.168.117.1/24 brd 192.168.117.255 scope global br0\n"
WAN = "3: eth3    inet 100.64.1.2/24 scope global eth3\n"
WG = "4: nwg0    inet 10.7.0.2/24 scope global nwg0\n"

def test_prefers_br0():
    assert lan_ipv4(WAN + WG + BR0, "default via 100.64.1.1 dev eth3") == "192.168.117.1"

def test_error_if_only_wan_and_wg():
    with pytest.raises(RuntimeError):
        lan_ipv4(WAN + WG, "default via 100.64.1.1 dev eth3")

def test_fallback_private_not_wan():
    lan = "5: br1    inet 192.168.1.1/24 scope global br1\n"
    assert lan_ipv4(WAN + lan, "default via 100.64.1.1 dev eth3") == "192.168.1.1"
```

- [ ] **Step 2: Run to verify fail**

Run: `cd scripts/geo-ui && python -m pytest backend/tests/test_bindaddr.py -v`

Expected: FAIL import.

- [ ] **Step 3: Implementation**

```python
from __future__ import annotations

import re
import subprocess

LINE_RE = re.compile(
    r"^\d+:\s+(\S+)\s+inet\s+(\d+\.\d+\.\d+\.\d+)/\d+"
)

def _is_private(ip: str) -> bool:
    a, b = (int(x) for x in ip.split(".")[:2])
    return a == 10 or (a == 192 and b == 168) or (a == 172 and 16 <= b <= 31)

def lan_ipv4(addr_text: str, route_text: str = "") -> str:
    wan = set()
    for m in re.finditer(r"\bdev\s+(\S+)", route_text):
        wan.add(m.group(1))
    parsed = []
    for line in addr_text.splitlines():
        m = LINE_RE.search(line)
        if not m:
            continue
        iface, ip = m.group(1), m.group(2)
        iface_base = iface.split("@")[0]
        parsed.append((iface_base, ip))
        if iface_base == "br0":
            return ip
    for iface, ip in parsed:
        if iface in ("lo", "nwg0") or iface in wan:
            continue
        if _is_private(ip):
            return ip
    raise RuntimeError("no LAN IPv4 (br0 / private) found")

def detect_lan_ipv4() -> str:
    addr = subprocess.check_output(["ip", "-4", "-o", "addr", "show"], text=True)
    try:
        route = subprocess.check_output(["ip", "route", "show", "default"], text=True)
    except subprocess.CalledProcessError:
        route = ""
    return lan_ipv4(addr, route)
```

- [ ] **Step 4: pytest PASS**

Run: `cd scripts/geo-ui && python -m pytest backend/tests/test_bindaddr.py -v`

Expected: PASS.

- [ ] **Step 5: Commit** — пропустить до просьбы.

---

### Task 5: apply stream

**Files:**
- Create: `scripts/geo-ui/backend/apply.py`
- Create: `scripts/geo-ui/backend/tests/test_apply.py`

**Interfaces:**
- Consumes: путь к `geo-update.sh`
- Produces:
  - `LOCK = "/opt/root/geo/geo-ui.lock"`
  - `iter_apply(script: str, lock_path: str) -> Iterator[tuple[str, object]]`
    — `("log", "строка без \\n")` затем `("done", {"exit": int})`
  - flock эксклюзивный на `lock_path` на всё время процесса
  - stdout+stderr merged; построчно

- [ ] **Step 1: Failing test с fake-скриптом**

```python
from pathlib import Path
from apply import iter_apply

def test_streams_lines_then_exit(tmp_path: Path):
    script = tmp_path / "upd.sh"
    script.write_text("#!/bin/sh\necho hello\necho world >&2\nexit 3\n", encoding="utf-8")
    script.chmod(0o755)
    lock = str(tmp_path / "lock")
    events = list(iter_apply(str(script), lock, extra_args=[]))
    kinds = [e[0] for e in events]
    assert kinds[-1] == "done"
    logs = [e[1] for e in events if e[0] == "log"]
    assert "hello" in logs and "world" in logs
    assert events[-1][1]["exit"] == 3
```

На Windows нет `chmod` + `sh`. Тест запускать через `sh` если есть Git Bash, иначе пометить `@pytest.mark.skipif(os.name=="nt" and not shutil.which("sh"))`. Для PC с Git: `iter_apply` должен запускать `[script]` если executable, иначе `["sh", script]`.

Проще: `iter_apply` всегда `Popen([exe] + extra_args)` где exe по умолчанию `/opt/root/geo/geo-update.sh`, extra_args default `["-n"]`. В тесте передать `exe=["python", str(py_script)]`:

```python
def test_streams_lines_then_exit(tmp_path: Path):
    py = tmp_path / "upd.py"
    py.write_text("import sys\nprint('hello', flush=True)\nprint('world', file=sys.stderr, flush=True)\nsys.exit(3)\n")
    events = list(iter_apply(cmd=["python", str(py)], lock_path=str(tmp_path / "lock")))
    ...
```

Сигнатура: `iter_apply(cmd: list[str] | None = None, lock_path: str = LOCK)` default cmd `["/opt/root/geo/geo-update.sh", "-n"]`.

- [ ] **Step 2: pytest fail** (нет `apply`)

- [ ] **Step 3: Implementation**

```python
from __future__ import annotations

import os
import subprocess
from collections.abc import Iterator

LOCK = "/opt/root/geo/geo-ui.lock"
DEFAULT_CMD = ["/opt/root/geo/geo-update.sh", "-n"]


def iter_apply(
    cmd: list[str] | None = None,
    lock_path: str = LOCK,
) -> Iterator[tuple[str, object]]:
    cmd = cmd or DEFAULT_CMD
    os.makedirs(os.path.dirname(lock_path) or ".", exist_ok=True)
    lockf = open(lock_path, "a+")
    try:
        if os.name == "nt":
            import msvcrt
            msvcrt.locking(lockf.fileno(), msvcrt.LK_LOCK, 1)
        else:
            import fcntl
            fcntl.flock(lockf.fileno(), fcntl.LOCK_EX)
        p = subprocess.Popen(
            cmd,
            stdout=subprocess.PIPE,
            stderr=subprocess.STDOUT,
            text=True,
            bufsize=1,
        )
        assert p.stdout is not None
        for line in p.stdout:
            yield ("log", line.rstrip("\n"))
        code = p.wait()
        yield ("done", {"exit": int(code)})
    finally:
        lockf.close()
```

- [ ] **Step 4: pytest PASS**

Run: `cd scripts/geo-ui && python -m pytest backend/tests/test_apply.py -v`

Expected: PASS.

- [ ] **Step 5: Commit** — пропустить до просьбы.

---

### Task 6: tags.py

**Files:**
- Create: `scripts/geo-ui/backend/tags.py`
- Create: `scripts/geo-ui/backend/tests/test_tags.py`

**Interfaces:**
- Produces: `list_tags(kind: str, dat: Path, geoview_output: str | None = None) -> list[str]`
- На роутере команда: `geoview -type <geosite|geoip> -action list -input <dat>` (если `-action list` нет в `-h` — взять фактический флаг list/dump из help и записать его в `tags.py` константой `LIST_ARGS`).
- Если geoview падает — `[]`, не 500.

Тест парсит фикстуру stdout (одна тег на строку, игнорировать пустые и `#`).

```python
def parse_tag_list(text: str) -> list[str]:
    out = []
    for line in text.splitlines():
        s = line.strip()
        if not s or s.startswith("#"):
            continue
        out.append(s.split()[0])
    return out
```

- [ ] **Steps 1–4:** тест `parse_tag_list`, реализация, pytest PASS. `list_tags` вызывает subprocess только если `geoview_output is None`.
- [ ] **Step 5: Commit** — пропустить.

---

### Task 7: Flask API

**Files:**
- Create: `scripts/geo-ui/backend/app.py`
- Create: `scripts/geo-ui/backend/tests/test_app.py`

**Interfaces:**
- Consumes: confio, collisions, apply.iter_apply, tags.list_tags, bindaddr.detect_lan_ipv4
- Env: `GEO_DIR` (default `/opt/root/geo`), `GEO_UI_DIST` (default рядом `../frontend/dist`), `GEO_UPDATE` cmd
- GET JSON:
  - `GET /api/groups` → `{ "groups": [{slug,title,description}], "collisions": [{value, hits: [[slug,set], ...]}] }`
  - `GET /api/groups/<slug>` → group + entries `{id, set, value, kind}`
  - `GET /api/groups/<slug>/entries` → `{ "entries": [...] }`
  - `GET /api/tags?type=geosite|geoip`
  - `GET /api/status` → `{ "last_exit": int|null, "counts": optional }`
- Мутации SSE после успешной записи. 400/404 JSON `{ "error": "..." }`.
- `id` = индекс в `group.entries`.
- POST group: `{title, description?}` → файл, `title_from_file=True`.
- PATCH group: частичный title/description; если выставили title — `title_from_file=True`.
- DELETE group: unlink файла.
- POST entry: validate classify_line; unknown tag — сохраняем (не 400).
- PATCH entry: `set` и/или `group` (slug цели).
- Статика: `/` и `/assets/*` из dist; если dist нет — 503 JSON на `/`, API живой.

SSE helper:

```python
import json
from flask import Response

def sse_apply():
    def gen():
        for kind, data in iter_apply():
            payload = data if kind == "log" else data
            if kind == "log":
                yield "event: log\ndata: %s\n\n" % json.dumps(data, ensure_ascii=False)
            else:
                yield "event: done\ndata: %s\n\n" % json.dumps(data)
    return Response(gen(), mimetype="text/event-stream")
```

После записи вызвать `sse_apply()`. Тесты API: `app.config["GEO_DIR"] = tmp_path`, мокать `iter_apply` чтобы не звать geo-update:

```python
@pytest.fixture
def client(tmp_path, monkeypatch):
    monkeypatch.setattr("apply.iter_apply", lambda **k: iter([("log", "ok"), ("done", {"exit": 0})]))
    from app import create_app
    app = create_app(geo_dir=tmp_path)
    return app.test_client()
```

Обязательные тесты: GET пустой список; POST group JSON... wait POST is SSE — после POST проверить файл + `mimetype` содержит `event-stream`; GET entries; POST invalid domain → 400 без файла-мусора; GET missing → 404.

`create_app(geo_dir, apply_cmd=None)` для тестов.

Flask bind **не** в `create_app`; `if __name__ == "__main__"` / init.d вызывает `detect_lan_ipv4()` и `app.run(host=ip, port=8787)`. Тест не слушает сокет.

- [ ] **Steps 1–4:** test_app.py → create_app → pytest PASS для GET/POST/PATCH/DELETE и 400.
- [ ] **Step 5: Commit** — пропустить.

---

### Task 8: Визуальный дизайн UI

**Files:**
- Create: `scripts/geo-ui/frontend/UI.md`

Spec требует проработать дизайн **до** вёрстки. Этот файл — контракт для Task 9. Не макет в Figma: конкретный layout, который реализуем.

Содержание `UI.md` — не черновик из этого плана, а файл `scripts/geo-ui/frontend/UI.md` после brainstorming 2026-08-20: сайдбар групп, лог всегда снизу, flex-секции в две колонки, инлайн-добавление в секцию, без кнопки «+ Секция».

- [x] **Step 1:** Создать `scripts/geo-ui/frontend/UI.md` (сделано при согласовании визуала).
- [x] **Step 2:** Пользователь утвердил макет (компаньон, «готово»).
- [ ] **Step 3: Commit** — пропустить.

---

### Task 9: Vue frontend

**Files:**
- Create: `scripts/geo-ui/frontend/package.json`
- Create: `scripts/geo-ui/frontend/vite.config.js`
- Create: `scripts/geo-ui/frontend/index.html`
- Create: `scripts/geo-ui/frontend/src/main.js`
- Create: `scripts/geo-ui/frontend/src/App.vue`
- Create: `scripts/geo-ui/frontend/src/api.js`
- Create: `scripts/geo-ui/frontend/src/style.css`

**Interfaces:**
- Vite `base: './'`, `server.proxy['/api']` → `http://127.0.0.1:8787` для dev на ПК.
- `api.js`: fetch GET JSON; мутации — `fetch` + `ReadableStream` / ручной parse SSE `event: log|done`.
- Поведение строго по `UI.md` + spec (не добавлять geo-why).

`package.json`:

```json
{
  "name": "geo-ui",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.4.0"
  }
}
```

`vite.config.js`:

```js
import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";
export default defineConfig({
  plugins: [vue()],
  base: "./",
  server: { proxy: { "/api": "http://127.0.0.1:8787" } },
});
```

`api.js` — функции: `getGroups`, `getEntries(slug)`, `createGroup`, `patchGroup`, `deleteGroup`, `addEntry`, `patchEntry`, `deleteEntry`, `getTags(type)` плюс `consumeSse(response, onLog)` → `Promise<number>` exit code.

App.vue: state `groups`, `slug`, `entries`, `collisions`, `logLines`, `busy`. При выборе группы — `GET .../entries`. После мутации — consume SSE в log panel, затем refresh groups+entries.

Коллизия для input: `collisions` где `value === norm(input)`.

- [ ] **Step 1:** scaffold + `npm install` в `scripts/geo-ui/frontend`.
- [ ] **Step 2:** `api.js` + `App.vue` по UI.md.
- [ ] **Step 3:** `npm run build` — появляется `frontend/dist/`.
- [ ] **Step 4: Commit** — пропустить.

---

### Task 10: init.d, запуск, проверка на роутере

**Files:**
- Create: `scripts/geo-ui/init.d/S80geo-ui`
- Create: `scripts/geo-ui/backend/run.py`

`run.py`:

```python
from bindaddr import detect_lan_ipv4
from app import create_app

def main():
    host = detect_lan_ipv4()
    app = create_app()
    app.run(host=host, port=8787, threaded=True)

if __name__ == "__main__":
    main()
```

`S80geo-ui` (Entware):

```sh
#!/bin/sh
# ENABLED=yes

ENABLED=yes
PROCS=python3
ARGS="/opt/root/geo-ui/backend/run.py"
PREARGS=""
DESC="geo-ui"
PATH=/opt/sbin:/opt/bin:/usr/sbin:/usr/bin:/sbin:/bin

. /opt/etc/init.d/rc.func
```

На роутере: `opkg install python3 python3-flask` (если `python3-flask` нет — `python3-pip` и `pip3 install -r requirements.txt -t /opt/root/geo-ui/backend/vendor` + `PYTHONPATH`). Скопировать `backend/` + `frontend/dist` + `geo-update.sh`. Симлинк `S80geo-ui`. `PYTHONPATH=/opt/root/geo-ui/backend`.

Проверка (spec):

1. pytest на ПК: `cd scripts/geo-ui && python -m pytest -v`
2. На роутере: временный `geo.d/cidr-test.conf`:
   ```
   # cidr-test
   [blocked-sites]
   1.2.3.4/24
   ```
   `geo-update.sh -n` → `ipset list blocked-sites-ip` содержит `1.2.3.4/24`. Затем удалить тестовый файл или оставить как группу.
3. `http://<br0>:8787` — создать группу, домен, тег, CIDR; лог как в консоли; exit 0.
4. Дубликат подсвечен, сохранить можно.
5. С WAN UI не открывается.
6. Файл без `#` — title = имя файла.
7. `S80geo-ui stop` / reboot / start.

- [ ] **Step 1:** `run.py` + init.d.
- [ ] **Step 2:** задеплоить и пройти чеклист.
- [ ] **Step 3: Commit** — пропустить до просьбы.

---

## Spec coverage (self-review)

| Spec | Task |
|---|---|
| LAN bind br0, не 0.0.0.0 | 4, 10 |
| Группа = файл, title/description | 1, 7 |
| Типы строк + CIDR без ip: | 1, 3 |
| geo-update *-ip без geoip: | 3, 10 |
| API list/get/entries + мутации SSE | 5, 7 |
| Коллизии warn-allow | 2, 9 |
| Vue dist с ПК | 8, 9 |
| init.d | 10 |
| geo-why v2 | не делаем |
| UI visual отдельно | 8 |
